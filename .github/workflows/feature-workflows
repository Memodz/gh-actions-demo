name: Workflow for Feature Branch
on:
 push:
   branchs: [feature/**]
jobs:
  build:
    runs-on : ubuntu-latest
  steps:
    -name: Check new featur quality
     run: echo "Checking the quality of the new feature branch"
